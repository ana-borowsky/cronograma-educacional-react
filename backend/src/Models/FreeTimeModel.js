class FreeTimeModel {
  constructor(
    idTime,
    idUser,
    weekDay,
    startTime,
    endTime
  ) {
    this.idTime = idTime,
    this.idUser = idUser,
    this.weekDay = weekDay,
    this.startTime = startTime,
    this.endTime = endTime
  }
}
export default FreeTimeModel